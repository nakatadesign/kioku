# kioku（記憶）

**A personal knowledge management template for Claude Code.**  
Capture → organize → recall. Proactively, safely.

English | [__日本語版 README はこちら__](./README.ja.md)

---

## Why kioku exists

If you use Claude Code seriously, you'll hit a wall.

The solution you found in yesterday's session is needed again today. The idea you had on your phone vanished by the time you opened your laptop. You're researching something you already looked up three months ago.

Claude Code is excellent in the moment. But **making knowledge persist across sessions** is something you have to build yourself.

kioku is a Git repository template that solves this.

It is not a tool. It is **a working directory for Claude Code to live in**.

---

## What kioku solves

| Problem | kioku's answer |
|---|---|
| Knowledge disappears when a session ends | Git persistence. Every session reads the same repository |
| Phone notes never make it into your workflow | Capture directly into Claude Code via Telegram |
| Past thinking stays buried | Proactive notifications surface connections: "This note links to something from 3 months ago" |
| Automation feels risky | 4-layer security design and full operation logs for post-hoc auditing |

---

## Capture from your computer or phone

kioku connects your PC and smartphone to a **single Git repository** — your one source of truth.

```
PC (Mac terminal) ──────────────────┐
                                     ▼
Smartphone (Telegram Channels) ──► Claude Code session
                                     ▼
                         your-username/kioku (Private)
                         ─────────────────────────────
                         inbox/ → notes/ → reports/
```

**Git is the single source of truth.** Write from any device, read from any machine — it's all the same repository.

---

## How knowledge flows

Every input lands in `inbox/` first.

```
① Input (from any device)
       │
       ▼
  inbox/YYYY-MM-DD-[type].md     ← auto-routed by trigger word
       │
       ▼
  Claude Code session ends
  → Stop hook: git commit (1 session = 1 commit)
  → proactive-check: scan for connections to existing notes
       │
       ▼
  /project:process (manual or scheduled)
  → read inbox/, classify, tag
  → organize and move to notes/[type]/
```

Prefix your message with a trigger word. Claude Code handles the rest.

| Message | Action |
|---|---|
| `idea: new feature concept` | appends to `inbox/YYYY-MM-DD-ideas.md` |
| `task: update docs by tomorrow` | appends to `inbox/YYYY-MM-DD-tasks.md` |
| `log: what I learned in this session` | appends to `inbox/YYYY-MM-DD-log.md` |
| `research: key points from this paper` | appends to `inbox/YYYY-MM-DD-research.md` |
| `save` | git commit + push |
| `status` | show today's inbox + git status |

---

## Repository structure

```
kioku/
├── CLAUDE.md                 # System constitution. Defines agent behavior.
├── .gitignore
│
├── .claude/
│   ├── settings.json         # Hook definitions (PostToolUse / Stop / SessionStart)
│   └── commands/
│       ├── capture.md        # /project:capture
│       ├── process.md        # /project:process (move inbox → notes/)
│       ├── daily.md          # /project:daily
│       └── push-related.md   # /project:push-related
│
├── inbox/                    # 📥 Landing zone for all input (the only entry point)
│   └── YYYY-MM-DD-[type].md
│
├── daily/                    # 📅 Daily notes (generated by process)
│   └── YYYY-MM-DD.md
│
├── notes/                    # 📚 Organized knowledge
│   ├── ideas/
│   ├── tasks/
│   ├── projects/
│   └── research/
│
├── reports/                  # 📊 Auto-generated summaries + proactive notification logs
│   ├── weekly-YYYY-WNN.md
│   └── proactive-YYYY-MM-DD.md
│
├── scripts/                  # 🔧 Automation scripts
│   ├── daily-summary.sh      # 09:00 daily — Telegram morning brief
│   ├── weekly-review.sh      # Sunday 10:00 — knowledge quality scoring
│   └── proactive-check.sh    # After each session — detect related notes
│
├── .launchd/                 # macOS scheduled tasks (no tmux required)
│   ├── daily.plist
│   └── weekly.plist
│
├── logs/                     # 📋 Execution logs for all scripts
│
└── docs/                     # Reference documentation (same visibility as this repo — see [Documentation](#documentation))
```

> **Note:** Auto-staging and auto-commit are driven by `.claude/settings.json` hooks, which only take effect when you open the kioku repository itself as your Claude Code working directory. If you open a different project as the root, kioku's hooks do not run — use `--add-dir` to reference kioku notes as read-only context from other sessions.

---

## Proactive notifications — the core of kioku

Most PKM systems answer when you ask. kioku **reaches out to you**.

### Two trigger modes

**Scheduled (launchd)**
- Daily at 09:00 — Telegram morning brief: inbox items, related notes, active tasks
- Sunday at 10:00 — Knowledge quality scoring (HIGH/MEDIUM/LOW) + weekly report

**Contextual (Stop hook)**
- After every session, new inbox items are cross-referenced against existing `notes/`
- If a connection is found, Telegram fires: "This idea may connect to something from 2 months ago"

### Notifications that don't become noise

A system that over-notifies gets ignored. kioku has governance built in from the start.

```bash
MAX_DAILY=3          # max notifications per day
MIN_MATCHES=2        # minimum matching notes required to notify
QUIET_START=22       # silent hours start (22:00–07:00: no notifications)
QUIET_END=7
NOTIFY_COOLDOWN=86400  # same note won't trigger again within 24 hours
EXCLUDE_TAGS="#private #nopush #draft"  # excluded from notification
```

One variable change is all it takes to adjust behavior. Every notification's reasoning is recorded in `reports/proactive-YYYY-MM-DD.md` so you can tune false positives with full traceability.

---

## How kioku compares

| | COG-second-brain | Claudsidian | brainqub3 | **kioku** |
|---|---|---|---|---|
| Obsidian required | recommended | Vault is the central UI (required) | no | **no** |
| Mobile input | none | none | none | **Telegram Channels** |
| Auto-commit | manual | manual | none | **Stop hook (1 session = 1 commit)** |
| Scheduled summaries | manual command | manual | none | **launchd fully automatic** |
| Proactive notifications | none | none | none | **scheduled + contextual triggers** |
| Notification governance | — | — | — | **limits, quiet hours, cooldown, exclude tags** |
| Notification log | — | — | — | **reports/proactive-*.md** |
| No tmux required | — | — | — | **✅ launchd only** |

> Claudsidian: [heyitsnoah/claudesidian](https://github.com/heyitsnoah/claudesidian) (★1,800)  
> COG-second-brain: [huytieu/COG-second-brain](https://github.com/huytieu/COG-second-brain) (★258)  
> Claude Code Channels: released March 20, 2026 as Research Preview

Channels support is a feature other templates can replicate. kioku's lasting differentiation is in **safe unattended operation** and **notification quality control** — both of which require deliberate design decisions, not just a plugin.

---

## Security design

kioku uses `--dangerously-skip-permissions` for unattended Channels operation. Four defensive layers compensate for this:

```
[allowlist]           only your Telegram ID can issue commands
    ↓
[WorkingDirectory]    launchd is scoped to kioku/ — can't wander elsewhere
    ↓
[CLAUDE.md rules]     rm -rf, git push --force, and other destructive ops are prohibited
    ↓
[logs/]               all script stdout/stderr written to logs/
                      → every action is auditable after the fact
```

Telegram is a third-party service. Notification messages contain only note titles and paths — never content, client names, amounts, or anything sensitive. Notes tagged `#private` are automatically excluded from all notifications.

---

## Cost estimate

| Purpose | Frequency | Cap/run | Monthly max |
|---|---|---|---|
| Daily summary | 30×/month | $0.30 | $9.00 |
| Weekly review | 4×/month | $1.00 | $4.00 |
| proactive-check | ~15×/month | $0.10 | $1.50 |
| **Total** | | | **$14.50 max** |

In practice, scripts rarely hit their caps. **$5–8/month** is a realistic estimate. This typically falls within Claude Max plan usage.

---

## Setup overview

Full instructions are in [`docs/setup-v1.md`](./docs/setup-v1.md).

**Prerequisites**
- Claude Code v2.1.80+
- Bun (required runtime for Telegram Channels plugin)
- Git / GitHub CLI
- Telegram account

**Five phases**

```
Phase 1 (Day 1, 15 min)   GitHub repo creation + directory initialization
Phase 2 (Day 1, 20 min)   CLAUDE.md — the instruction set for your agent
Phase 3 (Day 1, 15 min)   Hook configuration (.claude/settings.json)
Phase 4 (Day 1, 10 min)   Telegram Channels setup + pairing
Phase 5 (Day 2, 30 min)   Automation scripts + launchd registration
```

Phases 1–4 complete in a single day. Automation starts running the next morning.

---

## Using kioku alongside Obsidian

kioku doesn't compete with Obsidian. You can use kioku as a **capture-only layer** and hand off long-term storage to Obsidian.

```
iPhone → Telegram → kioku/inbox/    ← daily capture
kioku/notes/                         ← organized, short-term knowledge
      ↓ monthly export
Obsidian Vault                       ← long-term storage, graph view
```

---

## Optional: Semantic memory engine (v2)

kioku includes an optional local semantic search engine in `memory/`. When set up, it enables:

- **Meaning-based note discovery** — finds related notes even when keywords don't match, using a local embedding model ([Ruri v3-130m](https://huggingface.co/cl-nagoya/ruri-v3-130m))
- **Zero-cost proactive checks** — replaces the Claude API call in `proactive-check.sh` with local FTS5 + vector search
- **No external API dependency** — all search runs locally. Telegram notifications use the Bot API directly

This is entirely optional. Without it, kioku v1 works as-is with grep/glob search. To enable it:

```bash
bash scripts/memory-setup.sh
```

Requires Python 3.10+ with `enable_load_extension` support (e.g. Homebrew Python). See [`docs/memory-design-v2.md`](./docs/memory-design-v2.md) for architecture and setup details.

---

## Documentation

| Document | Contents |
|---|---|
| [Overview](./docs/overview-v1.md) | Design philosophy, comparisons, knowledge flow |
| [Architecture](./docs/architecture-v1.md) | Hook definitions, full scripts, Desktop sync rules |
| [Security](./docs/security-v1.md) | 4-layer defense, privacy policy, checklist |
| [Setup](./docs/setup-v1.md) | Phase 1–5 instructions, verification checklist |
| [Notification Policy](./docs/notification-policy-v1.md) | Governance settings, parameter tuning, log format |
| [Memory Design v2](./docs/memory-design-v2.md) | Semantic search engine architecture, local-first design |

---

## License

MIT

---

*kioku — 2026-03-23*
